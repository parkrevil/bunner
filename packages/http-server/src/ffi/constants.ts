import { FFIType } from 'bun:ffi';

/**
 * FFI Request Key Type
 * @description The FFI type for RequestKey, represented as a 64-bit unsigned integer.
 */
export const FFI_REQUEST_KEY_TYPE = FFIType.u64_fast;
