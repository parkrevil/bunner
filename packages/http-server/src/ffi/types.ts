/**
 * A type representing a unique identifier for an HTTP request.
 * @description This type is used to uniquely identify each HTTP request handled by the server.
 */
export type RequestKey = bigint;
