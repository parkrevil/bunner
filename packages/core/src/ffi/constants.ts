import { FFIType } from 'bun:ffi';

/**
 * Ffi App ID
 * @description The type for the FFI app ID
 */
export const FFI_APP_ID_TYPE = FFIType.u8;

/**
 * Ffi Worker ID
 * @description The type for the FFI worker ID
 */
export const FFI_WORKER_ID_TYPE = FFIType.u32;
